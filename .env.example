# Frontend env (Vite)
# Copy to `.env.development.local` for local dev (gitignored).
#
# Production builds receive these via build-time env, not from this file.

VITE_API_BASE_URL=http://localhost:3001/api/v1
VITE_WS_BASE_URL=ws://localhost:3001/ws
