import type { RouteObject } from "react-router-dom";

// Stub routes for Task 2. Task 3 will replace this with the real route tree.
export const routes: RouteObject[] = [
  { path: "/", element: null },
  { path: "*", element: null },
];
